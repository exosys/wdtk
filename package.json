{
  "name": "wdtk",
  "version": "0.0.1",
  "private": true,
  "description": "Development Toolkit",
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "ci": "yarn publish:main",
    "publish:canary": "lerna publish --canary --exact --bump=prerelease --no-git-tag-version",
    "publish:main": "lerna publish --skip-git --npm-tag beta",
    "~~release": "lerna version",
    "release:patch": "yarn ~~release patch",
    "changelog": "DO NOT USE -chan release --group-changes 0.0.1",
    "rel": "lerna version"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "~3.4.0",
    "@geut/chan": "^1.3.0",
    "typescript": "^3.1.3",
    "tslint": "^5.11.0",
    "eslint": "^5.7.0"
  }
}
